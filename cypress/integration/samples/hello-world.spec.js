describe("Sample test", () => {
  it("A sample test", () => {
    /*
    // Login the user
    cy.login("someemail@retool.com", "somepassword");
    cy.selectFolder("Folder that contains app");
    cy.viewApp("App to test");

    // Expect a normal input's value to be an empty string
    cy.getComponent("textinput1").should("have.value", "");

    // Type input and make assertions
    cy.getComponent("textinput1").type("10");
    cy.getTableCell("table1", { column: 6, row: 0 }).should("contain", "10");
    cy.getTableCell("table1", { column: 6, row: 1 }).should("contain", "20");
    cy.getComponent("textinput1").type("0");
    cy.getTableCell("table1", { column: 6, row: 0 }).should("contain", "100");
    cy.getTableCell("table1", { column: 6, row: 1 }).should("contain", "200");
    */
  });
});
